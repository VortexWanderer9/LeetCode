// fid if object is empty or not

const isObjectEmpty = (obj) => {
    return Object.keys(obj).length === 0
}
var  ObjectEmpty = (obj) => {
    for(key in obj) {
        return false
    }
    return true
} 


